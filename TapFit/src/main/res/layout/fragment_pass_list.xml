<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent" android:padding="@dimen/padding">

    <ListView
        android:id="@+id/pass_list"
        android:layout_height="match_parent"
        android:layout_width="match_parent"
        android:background="@drawable/list_view_background_round"/>

    <TextView
        android:id="@+id/no_pass_text"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:gravity="center"
        android:padding="10dp"
        android:text="You have no passes. Once you start using TapFit, your passes will be shown here."
        style="@style/Font"
        android:textColor="@color/black"
        android:visibility="gone" />

</LinearLayout>